#include <bits/stdc++.h>
using namespace std;
int main() {
    int a, b, c, d, result;
    cin >> a >> b >> c >> d;
    while (a !=0 && b != 0 && c != 0 && d != 0) {
        result = c - b;
        cout << result << endl;
    }
    return 0;
}